# assets/

Images referenced by top-level project docs (currently just the README's
header image). Not used by the app itself — if that ever changes, split
app-referenced assets into their own tracked location instead of mixing them
in here, same reasoning as `icons/`'s own README.
