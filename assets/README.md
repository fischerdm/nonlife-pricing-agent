# assets/

Images referenced by top-level project docs. Not used by the app itself — if
that ever changes, split app-referenced assets into their own tracked
location instead of mixing them in here, same reasoning as `icons/`'s own
README.

| File | Used in | Description |
|------|---------|-------------|
| `teaser.png` | `README.md` (top) | Dashboard screenshots: Feature & Grouping Workbench (target-leakage warning), GLM Distillation Workbench (auto-excluded terms), GLM coefficient review (Fit History, Rating Factors) |
